# sol/main divergence from develop

Every commit on `sol/main` that is not on `origin/develop` is listed here with its disposition. If it is not in this table, it should not be on the branch. Updated by Sol on every merge into `sol/main`.

| Area | Branch(es) | Disposition | Upstream PR |
|---|---|---|---|
| `wakesync/` lane docs + receipts | sol/main | never upstream, stripped from PRs | n/a |

Dispositions: `upstream-pending` (will be cut as a PR), `upstream-open` (PR link), `upstream-merged` (drop from sol/main on next rebase), `sol-only` (never upstream: character, secrets wiring, personal config).
